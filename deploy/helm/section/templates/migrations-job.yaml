{{- if .Values.migrations.enabled }}
# Pre-upgrade migrations Job. Runs the SQL files mounted from a ConfigMap
# against the configured Postgres database. Hook weight ensures it runs
# before any Deployments/StatefulSets are upgraded.
#
# NOTE: this is deliberately `pre-upgrade` only, NOT `pre-install`. Helm runs
# pre-install hooks before any of the release's regular resources exist, so on
# a fresh install the Job pod would reference a ServiceAccount and a Secret
# (database-url) that have not been created yet — the pod never starts and
# `helm install --wait` sits until it reports "failed pre-install: timed out
# waiting for the condition". With embedded Postgres there is also no database
# to migrate against at that point. Fresh installs are covered by the gateway
# entrypoint, which runs `alembic upgrade head` (retrying while Postgres comes
# up) unless SECTION_AUTO_MIGRATE=0.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "section.fullname" . }}-migrations
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "section.componentLabels" (dict "ctx" . "component" "migrations") | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
data:
  {{- $root := . }}
  {{- range $path, $_ := .Files.Glob "files/migrations/*.sql" }}
  {{ base $path }}: |-
    {{- $root.Files.Get $path | nindent 4 }}
  {{- end }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "section.fullname" . }}-migrate
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "section.componentLabels" (dict "ctx" . "component" "migrations") | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.migrations.backoffLimit }}
  activeDeadlineSeconds: {{ .Values.migrations.activeDeadlineSeconds }}
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        {{- include "section.componentSelectorLabels" (dict "ctx" . "component" "migrations") | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "section.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: migrate
          image: "{{ .Values.migrations.image.repository }}:{{ .Values.migrations.image.tag }}"
          imagePullPolicy: IfNotPresent
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          env:
            # psql parses the DSN; gateway uses asyncpg DSN with a +asyncpg
            # driver suffix that psql cannot handle, so we strip it.
            - name: RAW_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "section.gatewaySecretName" . }}
                  key: database-url
          command:
            - /bin/sh
            - -ec
            - |
              # Strip SQLAlchemy +asyncpg driver suffix for psql compatibility.
              CLEAN_URL=$(printf '%s' "$RAW_DATABASE_URL" | sed 's#postgresql+asyncpg://#postgresql://#')
              for f in /migrations/*.sql; do
                echo "Applying $f"
                psql "$CLEAN_URL" -v ON_ERROR_STOP=1 -f "$f"
              done
          volumeMounts:
            - name: migrations
              mountPath: /migrations
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
      volumes:
        - name: migrations
          configMap:
            name: {{ include "section.fullname" . }}-migrations
        - name: tmp
          emptyDir: {}
{{- end }}
